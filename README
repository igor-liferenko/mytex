To see all PK files for <font> (assuming that <font> is not contained in MFinputs),
used in <document>, do:
rm -f ~/tex/TeXfonts/<mode>/<font>.*
tex? <document> (`?' = `+' for <mode> `screen', `?' = `-' for <mode> `localfont')
