#!/bin/sh
pkill -KILL -x gv
PATH=~/mytex/bin:$PATH
gv -geometry $(~/mytex/screen-size) $1 2>/dev/null & # 2>/dev/null avoids "The X11 connection broke" error on terminal when closing with right mouse button
